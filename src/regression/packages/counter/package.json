{
  "name": "counter",
  "description": "counter",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "aewasm build && mkdir -p ../../../../priv/regression/counter && cp dist/contract.wasm ../../../../priv/regression/counter/contract.wasm && cp dist/manifest.json ../../../../priv/regression/counter/manifest.json && rm -rf dist",
    "test": "npm run build && vitest"
  },
  "dependencies": {
    "@archethicjs/ae-contract-as": "github:pasteque-org/ae-contract-as",
    "@archethicjs/ae-contract-test": "^1.0.0",
    "assemblyscript": "^0.27.29"
  },
  "devDependencies": {
    "visitor-as": "^0.11.4",
    "@types/node": "^22.1.0",
    "vitest": "^2.1.1",
    "typescript": "^5.5.4"
  },
  "overrides": {
    "assemblyscript": "$assemblyscript"
  }
}
